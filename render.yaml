buildCommand: bash render-build.sh
```

---

## 🧪 SAU KHI SỬA - KIỂM TRA:

### 1. Push lên GitHub
- File đã commit xong

### 2. Đợi Render deploy
- Vào Render Dashboard
- Xem **Logs** tab
- Tìm dòng:
```
📦 Installing system dependencies for canvas...
✅ Build complete!
🧪 Testing Canvas module...
   ✅ Canvas test: OK
```

### 3. Test trên Discord
```
.tx
# Đặt cược
# Đợi 30 giây
# Phải thấy ảnh 3 xúc xắc!
```

---

## 🔍 DEBUG NẾU VẪN LỖI:

### Check Render Logs cho các dòng:
```
❌ Canvas test FAILED: ...           ← Canvas không load được
✅ Canvas test: OK                   ← Canvas OK
⚠️ Canvas failed, sending without image  ← Vẽ xúc xắc thất bại
```

### Nếu thấy lỗi Canvas:
```
Error: Canvas module not found
Error: libcairo.so.2: cannot open shared object file
